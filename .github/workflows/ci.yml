name: Proton CI

on:
  push:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: write

jobs:
  pre-commit:
    runs-on: ubuntu-latest

    container:
      image: ubuntu:24.04

    steps:
      - name: Install dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            git \
            python3 \
            python3-venv \
            python3-pip

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Mark safe repository
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: venv setup
        run: |
          python3 -m venv venv
          . venv/bin/activate
          pip install --upgrade pip
          pip install pre-commit

      - name: Run pre-commit
        run: |
          . venv/bin/activate
          pre-commit run --all-files

  build-and-test:
    runs-on: ubuntu-latest

    container:
      image: ubuntu:24.04

    steps:
      - name: Install dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            build-essential \
            cmake \
            pkg-config \
            python3 \
            python3-pip \
            python3-jinja2 \
            python3-yaml \
            python3-protobuf \
            protobuf-compiler \
            libboost-all-dev \
            libyaml-cpp-dev \
            libgtest-dev \
            git \
            lcov

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Configure CMake
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPROTON_ENABLE_TEST_COVERAGE=ON

      - name: Build
        run: |
          cmake --build build --parallel

      - name: Run tests
        run: |
          ctest --test-dir build/tests --output-on-failure

      - name: Generate tests coverage report
        run: |
          lcov --directory build --capture --output-file tests_coverage.info --ignore-errors mismatch
          lcov --remove tests_coverage.info "*tests/*" "examples/*" "core/*" "/usr/include/*" "*/_deps/*" --output-file tests_coverage.info --ignore-errors unused
          genhtml tests_coverage.info --output-directory tests_coverage_report --title "Proton Tests Code Coverage"

      - name: Generate tests coverage summary
        run: |
          lcov --summary tests_coverage.info > tests_coverage_summary.txt

      - name: Upload tests coverage report
        uses: actions/upload-artifact@v4
        with:
          name: tests-coverage-report
          path: tests_coverage_report/
          retention-days: 30

      - name: Upload tests coverage summary
        uses: actions/upload-artifact@v4
        with:
          name: tests-coverage-summary
          path: tests_coverage_summary.txt
          retention-days: 30

  test-proton-core:
    runs-on: ubuntu-latest

    container:
      image: ubuntu:24.04

    steps:
      - name: Install dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            build-essential \
            cmake \
            pkg-config \
            python3 \
            python3-pip \
            python3-jinja2 \
            python3-yaml \
            python3-protobuf \
            protobuf-compiler \
            libboost-all-dev \
            libyaml-cpp-dev \
            libgtest-dev \
            git \
            lcov

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Configure CMake
        run: |
          cmake -S ./core -B build_core \
            -DCMAKE_BUILD_TYPE=Debug \
            -DPROTON_BUILD_TESTS=ON \
            -DPROTON_ENABLE_TEST_COVERAGE=ON

      - name: Build
        run: |
          cmake --build build_core --parallel

      - name: Run tests
        run: |
          ctest --test-dir build_core --output-on-failure

      - name: Generate core coverage report
        run: |
          lcov --directory build_core --capture --output-file core_coverage.info --ignore-errors mismatch
          lcov --remove core_coverage.info "*tests/*" "/usr/include/*" "*/_deps/*" --output-file core_coverage.info --ignore-errors unused
          genhtml core_coverage.info --output-directory core_coverage_report --title "Proton Core Code Coverage"

      - name: Generate core coverage summary
        run: |
          lcov --summary core_coverage.info > core_coverage_summary.txt

      - name: Upload core coverage report
        uses: actions/upload-artifact@v4
        with:
          name: core-coverage-report
          path: core_coverage_report/
          retention-days: 30

      - name: Upload core coverage summary
        uses: actions/upload-artifact@v4
        with:
          name: core-coverage-summary
          path: core_coverage_summary.txt
          retention-days: 30

  comment-coverage-on-pr:
    if: github.event_name == 'pull_request'
    needs: [build-and-test, test-proton-core]
    runs-on: ubuntu-latest

    steps:
      - name: Download PR head tests coverage summary
        uses: actions/download-artifact@v8
        with:
          name: tests-coverage-summary
          path: coverage_summaries/head

      - name: Download PR head core coverage summary
        uses: actions/download-artifact@v8
        with:
          name: core-coverage-summary
          path: coverage_summaries/head

      - name: Find base workflow run id
        id: find_base_run
        uses: actions/github-script@v8
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const baseBranch = context.payload.pull_request.base.ref;
            const baseSha = context.payload.pull_request.base.sha;

            let page = 1;
            let runId = '';

            while (true) {
              const { data } = await github.rest.actions.listWorkflowRuns({
                owner,
                repo,
                workflow_id: 'ci.yml',
                event: 'push',
                branch: baseBranch,
                per_page: 100,
                page,
              });

              const match = data.workflow_runs.find((run) => run.head_sha === baseSha);
              if (match) {
                runId = String(match.id);
                break;
              }

              if (data.workflow_runs.length < 100) {
                break;
              }
              page += 1;
            }

            core.info(`Base branch: ${baseBranch}`);
            core.info(`Base SHA: ${baseSha}`);
            core.info(`Resolved run ID: ${runId || 'not found'}`);
            core.setOutput('run_id', runId);

      - name: Download base tests coverage summary artifact
        if: steps.find_base_run.outputs.run_id != ''
        uses: actions/download-artifact@v8
        with:
          name: tests-coverage-summary
          path: coverage_summaries/base
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ steps.find_base_run.outputs.run_id }}

      - name: Download base core coverage summary artifact
        if: steps.find_base_run.outputs.run_id != ''
        uses: actions/download-artifact@v8
        with:
          name: core-coverage-summary
          path: coverage_summaries/base
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ steps.find_base_run.outputs.run_id }}

      - name: Build PR coverage comment body
        run: |
          extract_rate() {
            local metric="$1"
            local file="$2"
            if [[ -f "$file" ]]; then
              grep -E "^ *${metric}\\.+:" "$file" | sed -E 's/.*: *([0-9]+(\\.[0-9]+)?)%.*/\1/' | head -1
            else
              echo ""
            fi
          }

          format_percent() {
            local value="$1"
            if [[ -n "$value" ]]; then
              echo "${value}%"
            else
              echo "N/A"
            fi
          }

          format_delta() {
            local head_rate="$1"
            local base_rate="$2"
            if [[ -z "$head_rate" || -z "$base_rate" ]]; then
              echo "N/A"
            else
              awk -v h="$head_rate" -v b="$base_rate" 'BEGIN { d = h - b; if (d > 0) printf("+%.2f", d); else printf("%.2f", d); }'
            fi
          }

          head_tests_lines=$(extract_rate lines coverage_summaries/head/tests_coverage_summary.txt)
          head_tests_funcs=$(extract_rate functions coverage_summaries/head/tests_coverage_summary.txt)
          head_core_lines=$(extract_rate lines coverage_summaries/head/core_coverage_summary.txt)
          head_core_funcs=$(extract_rate functions coverage_summaries/head/core_coverage_summary.txt)

          base_tests_lines=$(extract_rate lines coverage_summaries/base/tests_coverage_summary.txt)
          base_tests_funcs=$(extract_rate functions coverage_summaries/base/tests_coverage_summary.txt)
          base_core_lines=$(extract_rate lines coverage_summaries/base/core_coverage_summary.txt)
          base_core_funcs=$(extract_rate functions coverage_summaries/base/core_coverage_summary.txt)

          delta_tests_lines=$(format_delta "$head_tests_lines" "$base_tests_lines")
          delta_tests_funcs=$(format_delta "$head_tests_funcs" "$base_tests_funcs")
          delta_core_lines=$(format_delta "$head_core_lines" "$base_core_lines")
          delta_core_funcs=$(format_delta "$head_core_funcs" "$base_core_funcs")

          {
            echo "<!-- proton-coverage-comment -->"
            echo "## Coverage Summary"
            echo
            echo "| Scope | Base Lines | PR Lines | Delta (pp) | Base Functions | PR Functions | Delta (pp) |"
            echo "|---|---:|---:|---:|---:|---:|---:|"
            echo "| protonc/protoncpp | $(format_percent "$base_tests_lines") | $(format_percent "$head_tests_lines") | ${delta_tests_lines} | $(format_percent "$base_tests_funcs") | $(format_percent "$head_tests_funcs") | ${delta_tests_funcs} |"
            echo "| core | $(format_percent "$base_core_lines") | $(format_percent "$head_core_lines") | ${delta_core_lines} | $(format_percent "$base_core_funcs") | $(format_percent "$head_core_funcs") | ${delta_core_funcs} |"
            echo
            echo "Coverage deltas are computed as: PR source branch minus target branch (base commit)."
            echo
            if [[ ! -f coverage_summaries/base/tests_coverage_summary.txt || ! -f coverage_summaries/base/core_coverage_summary.txt ]]; then
              echo "Baseline artifact for the target branch commit was not found, so one or more deltas may be shown as N/A."
              echo
            fi
            echo "Detailed HTML reports are available in the workflow artifacts:"
            echo "- tests-coverage-report"
            echo "- core-coverage-report"
          } > coverage_comment.md

      - name: Create or update PR coverage comment
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('coverage_comment.md', 'utf8');
            const marker = '<!-- proton-coverage-comment -->';
            const issue_number = context.issue.number;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              per_page: 100,
            });

            const existing = comments.find((c) => c.body && c.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number,
                body,
              });
            }

  test-proton-vendor:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

    container:
      image: rostooling/setup-ros-docker:ubuntu-noble-latest

    steps:
      - uses: actions/checkout@v6

      - name: Create proton_vendor repos file
        run: |
          cat > /tmp/proton_vendor.repos <<EOF
          repositories:
            proton_vendor:
              type: git
              url: https://github.com/clearpathrobotics/proton_vendor.git
              version: main
          EOF

      - uses: ros-tooling/action-ros-ci@v0.4
        id: action_ros_ci_step
        with:
          target-ros2-distro: jazzy
          package-name: proton_vendor
          vcs-repo-file-url: file:///tmp/proton_vendor.repos
